---
import { catalogItems } from '../config/site';
---

<section id="catalogo" class="relative py-16 md:py-24">
  <div class="pointer-events-none absolute inset-0 -z-10">
    <div class="absolute left-1/4 top-6 h-44 w-44 rounded-full bg-amber-500/10 blur-3xl"></div>
  </div>
  <div class="container-main surface-panel px-4 py-10 md:px-8 md:py-12">
    <!-- Encabezado -->
    <div class="mx-auto max-w-2xl text-center">
      <span class="text-xs font-bold uppercase tracking-[0.2em] text-amber-400">Catálogo</span>
      <h2 class="mt-3 font-display text-3xl font-bold text-white md:text-4xl lg:text-5xl">
        Explora todo lo que puedes ver
      </h2>
      <p class="mt-4 text-base text-zinc-100">
        Desde estrenos de Hollywood hasta la liga local. Todo en un solo lugar, actualizado cada semana.
      </p>
    </div>

    <div class="line-accent mx-auto mt-8 w-24"></div>

    <!-- Grid de categorías -->
    <div class="mt-12 grid gap-4 sm:grid-cols-2">
      {catalogItems.map((item, i) => (
        <article class:list={[
          'group relative overflow-hidden rounded-2xl border border-white/20 bg-slate-900/35',
          i === 0 || i === 3 ? 'sm:col-span-1' : '',
        ]}>
          <!-- Imagen de fondo -->
          <img
            src={item.image}
            alt={item.title}
            class="h-64 w-full object-cover transition-transform duration-700 group-hover:scale-105 sm:h-72"
            loading="lazy"
          />

          <!-- Overlay gradiente -->
          <div class="absolute inset-0 bg-gradient-to-t from-dark-950 via-dark-950/70 to-transparent"></div>

          <!-- Contenido -->
          <div class="absolute inset-x-0 bottom-0 p-6">
            <span class="inline-block rounded-full bg-rose-600/20 px-3 py-1 text-xs font-bold uppercase tracking-wider text-rose-300">
              {item.count}
            </span>
            <h3 class="mt-3 font-display text-2xl font-bold text-white">{item.title}</h3>
            <p class="mt-2 text-sm leading-relaxed text-zinc-200">{item.desc}</p>
          </div>
        </article>
      ))}
    </div>

    <!-- Texto de cierre -->
    <p class="mt-8 text-center text-sm text-zinc-200/90">
      Contenido actualizado semanalmente. Películas de estreno, temporadas completas y eventos deportivos en vivo.
    </p>
  </div>
</section>
